@model FashionVote.Models.Show

@{
    ViewData["Title"] = "Confirm Delete";
}

<h2>Confirm Delete</h2>

<p>Are you sure you want to delete the show "<strong>@Model.ShowName</strong>"?</p>

@* <form asp-action="DeleteConfirmedParticipant" method="post">
    <input type="hidden" name="showId" value="@Model.ShowId" />
    <button type="submit" class="btn btn-danger">Confirm Delete</button>
    <a asp-action="MyShows" class="btn btn-secondary">Cancel</a>
</form> *@

<form asp-action="DeleteConfirmedParticipant"
      asp-controller="Shows"
      asp-route-showId="@Model.ShowId"
      method="post">
    <input type="hidden" name="showId" value="@Model.ShowId" />
    <button type="submit" class="btn btn-danger">Confirm Delete</button>
    <a asp-action="MyShows" class="btn btn-secondary">Cancel</a>
</form>


